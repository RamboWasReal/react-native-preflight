# 0.2.0 Release Prep

## Scope

- Preserve raw Maestro stderr when a per-file Maestro run exits non-zero without a parseable summary.
- Keep scenario wrapper `testID`s visible to native E2E tools with `collapsable={false}`.
- Prepare changelog notes for `0.2.0`.

## Decisions

- Leave `package.json` at `0.1.5`; the minor version bump will be done separately.
- Keep the Maestro diagnostic fix in the CLI test command path.
- Commit release prep separately from behavior changes.

## Verification

- `yarn test:all`
